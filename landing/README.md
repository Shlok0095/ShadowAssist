# ShadowAssist landing (static)

1. `site-config.js` is set for **Shlok0095/ShadowAssist** — update URLs if the repo name or default branch changes; bump installer filename when you release a new `package.json` version.
2. Optionally copy `../legal/*.txt` into `legal/` here if you deploy without using raw GitHub URLs.
3. Upload this folder’s contents to your static host (no build step).

See `../docs/LAUNCH_END_TO_END.md` for the full release workflow.
