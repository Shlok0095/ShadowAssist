# Quarantine folder

Files moved here during v1.0.0 cleanup (2026-06-26). Nothing is permanently deleted — restore by moving back to the original path listed in each subfolder's `manifest.txt`.

**Do not ship this folder in releases.** It is excluded from builds via `.gitignore` patterns where applicable; only manifests may be committed if needed for audit.
